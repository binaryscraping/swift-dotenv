# `swift-dotenv`

Load `.env` files in Swift.


## Installation

### Make
```sh
$ git clone https://github.com/binaryscraping/swift-dotenv
$ cd swift-dotenv
$ make install
```

Usage:
```sh
$ swift-dotenv -h
```
